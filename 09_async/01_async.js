// a function that changes the text of heading after two second -asynchronus function
// settimeout function
let heading = document.getElementById("heading")
let stopBtn = document.getElementById("stop")
// const changingText = setTimeout(() => {
//     heading.innerHTML = "And His Name Is Shivam"
// }, 2000);

// // a function that stops changing the text when clicked on stop button
// stopBtn.addEventListener("click", (e) => {
//     clearTimeout(changingText)
// })


// setInterval and clearInterval

// const writeIntervalText = setInterval(() => {
//     document.write("Hello I am interval function")
// }, 2000);

// setTimeout(() => {
//     clearInterval(writeIntervalText)
// }, 4000);